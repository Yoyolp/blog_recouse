---
title: heap1
---

malloc

<!--more-->

`malloc` 本省并不是系统调用,而是`用户态内存分配器`，底层依赖操作系统提供的内存管理接口`brk`/`sbrk` `mmap` 向内存申请内存，再通过用户态逻辑（如空闲链表，内存池）高效管理内存

## 堆
堆，是进程地址空间中一块动态内存分配的连续区域，其中的数据在程序运行中会随时被添加或者删除， 而在Linux 内核中 chch














